[project]
name = "notification-watcher"
version = "1.1.0"
description = "Watch OS notifications and forward to Trade Platform ingest"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
